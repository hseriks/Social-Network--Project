

// this hole section might not be needed as I have the reducer 


//this give back everything from friendships

// export const fetchFriendsAndRequests = (state) => state.friendships;

// //this give back list of users from friendships where accepted is true

// export const fetchFriends = (state) =>
//     state.friendships.filter((user) => user.accepted === true);

// //this give back list of users from friendships where accepted is false

// export const fetchPending = (state) =>
//     state.friendships.filter((user) => user.accepted === false);